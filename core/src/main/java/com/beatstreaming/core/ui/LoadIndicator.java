package com.beatstreaming.core.ui;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.ProgressBar;

public class LoadIndicator extends ProgressBar {
    public LoadIndicator(Context context) {
        super(context);
    }

    public LoadIndicator(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public LoadIndicator(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public LoadIndicator(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
